# Required for OpenRouter proxy mode: Your OpenRouter API key
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Required for Anthropic proxy mode (Claude Code, etc.): Your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: OpenRouter model to use (default: meta-llama/llama-4-maverick:free)
OPENROUTER_MODEL=meta-llama/llama-4-maverick:free

# Optional: Server port (default: 4343)
PORT=4343

# Optional: Comma-separated glob patterns for files to watch
# Default: **/*.js,**/*.ts,**/*.mjs,**/*.ejs,**/*.html,**/*.css
WATCH_PATTERNS=**/*.js,**/*.ts,**/*.mjs,**/*.ejs,**/*.html,**/*.css

# Optional: Comma-separated glob patterns for files/directories to ignore
# Default: node_modules/**,dist/**,build/**,summaries/**
WATCH_IGNORE=node_modules/**,dist/**,build/**,summaries/**

# Examples for other languages:
# Python: WATCH_PATTERNS=**/*.py
# Go: WATCH_PATTERNS=**/*.go
# Ruby: WATCH_PATTERNS=**/*.rb
# Multi-language: WATCH_PATTERNS=**/*.js,**/*.py,**/*.go,**/*.md
